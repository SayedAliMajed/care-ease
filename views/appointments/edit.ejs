<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/style.css" />
  <title>Edit Appointment</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>

  <main class="container">
    <h1>Edit Appointment</h1>
    <form action="/appointments/<%= appointment._id %>?_method=PUT" method="POST">
      <label for="cpr">CPR</label>
      <input
        type="number"
        id="cpr"
        name="cpr"
        pattern="[0-9]{9}"
        maxlength="9"
        minlength="9"
        value="<%= appointment.cpr %>"
        required
        title="CPR must be exactly 9 digits"
        />

      <label for="appointmentDate">Date</label>
      <input
        type="date"
        id="appointmentDate"
        name="date"
        value="<%= appointment.date.toISOString().slice(0, 10) %>"
        required
      />

      <label for="appointmentTime">Time</label>
      <input
        type="time"
        id="appointmentTime"
        name="time"
        value="<%= appointment.time %>"
        required
      />

      <label for="status">Status</label>
      <select name="status" id="status" required>
        <option value="scheduled" <%= appointment.status === 'scheduled' ? 'selected' : '' %>>
          Scheduled
        </option>
        <option value="completed" <%= appointment.status === 'completed' ? 'selected' : '' %>>
          Completed
        </option>
        <option value="cancelled" <%= appointment.status === 'cancelled' ? 'selected' : '' %>>
          Cancelled
        </option>
      </select>

      <% if (user.role !== 'patient') { %>
        <label for="duration">Duration</label>
        <input type="number" id="duration" name="duration" value="<%= appointment.duration || 30 %>" min="5" required />

        <label for="prescription">Prescription</label>
        <textarea id="prescription" name="prescription" rows="5"><%= appointment.prescription || '' %></textarea>
      <% } %>
      
      <button type="submit">Update Appointment</button>
    </form>
  </main>
</body>
</html>
