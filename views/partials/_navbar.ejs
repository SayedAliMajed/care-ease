<nav>
  <% if (user && user.profile && user.profile.fullName) { %>
    <p><strong>Welcome, <%= user.profile.fullName %></strong></p>

    <% if (user.role === 'patient') { %>
      <a href="/appointments">View My Appointments</a>
    <% } else if (user.role === 'doctor') { %>
      <a href="/admin/doctor">Doctor Dashboard</a>
      <a href="/appointments">Manage Appointments</a>
      <a href="/availabilitys">Availability Management</a>
    <% } else if (user.role === 'employee') { %>
      <a href="/admin/employee">Employee Dashboard</a>
      <a href="/appointments">Manage Appointments</a>
      <a href="/availabilitys">Availability Management</a>
    <% } else if (user.role === 'admin') { %>
      <a href="/admin/dashboard">Admin Dashboard</a>
      <a href="/appointments">All Appointments</a>
      <a href="/admin/users">Manage Users</a>
      <a href="/availabilitys">Availability Management</a>
    <% } %>

    <a href="/auth/sign-out">Sign Out</a>

  <% } else { %>
    <a href="/auth/sign-up">Sign Up</a>
    <a href="/auth/sign-in">Sign In</a>
  <% } %>
</nav>
