<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title>Edit Availability</title>
</head>

<body>
  <%- include('../partials/_navbar.ejs') %>
  <main class="container">
    <h2>Edit Availability</h2>
    <form action="/availabilitys/<%= availability._id %>?_method=PUT" method="POST">
      <label>Date:
        <input type="date" name="date" value="<%= new Date(availability.date).toISOString().substring(0,10) %>" required />
      </label>

      <label>Opening Time:
        <input type="time" name="openingTime" value="<%= availability.openingTime %>" required />
      </label>

      <label>Closing Time:
        <input type="time" name="closingTime" value="<%= availability.closingTime %>" required />
      </label>

      <label>Slot Duration (minutes):
        <input type="number" name="duration" value="<%= availability.duration %>" min="1" required />
      </label>

      <label>Break Start Time (optional):
        <input type="time" name="breakStartTime" value="<%= availability.breakStartTime || '' %>" />
      </label>

      <label>Break End Time (optional):
        <input type="time" name="breakEndTime" value="<%= availability.breakEndTime || '' %>" />
      </label>

      <button type="submit" class="button-primary">Save Changes</button>
    </form>
    <a href="/availabilitys" class="button-outline">Back to List</a>
  </main>
   <%- include('../partials/_footer') %>
</body>

</html>
